import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles({
  listSize: {
    maxWidth: "800px",
    margin: "50px auto",
    padding: 32,
  },
});

export default styles;
